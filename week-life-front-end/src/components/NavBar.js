import React from 'react'
import { Authentication } from './Authentication'

export const NavBar = () => {
    return (
        <div>
            <nav>
                <Authentication />
            </nav>
        </div>
    )
}
